# 2026-03-30 Session Log

## Work Done

### AR Layout Fixes — portal/portal.css (all pushed to main)
- **Thread title (discussions.html):** .thread-title is an inline <span> — added width:100% to both html.ar .thread-title rules so text-align:right has a container to work against.
- **Resource card:** Had ow-reverse + explicit direction:rtl on the card itself → double-cancel. Removed the card-level direction:rtl. ow-reverse remains.
- **Event card:** Same ow-reverse issue. Stripped ow-reverse, then discovered direction:rtl alone does NOT flip flex order — only text/inline. Restored ow-reverse. Multiple commits back and forth — messy session.
- **Founder card / founder-card-header:** Same row-reverse restore.
- **Root lesson (add to GOTCHAS):** direction:rtl on a flex container does NOT reverse visual order of flex children. lex-direction:row-reverse IS required for that. The original resource card bug was ow-reverse + card-level direction:rtl = double-cancel. Fix = remove card-level direction:rtl, keep ow-reverse.

### Merge
- Merged 
ightly/2026-03-30 → main mid-session at operator request.
- All subsequent fixes committed directly to main.

## Current Git State
- Branch: main, clean, up to date with origin
- Last commit: 66b1867 — restore flex-direction:row-reverse on AR event/resource/founder cards

## Open / Unresolved
- Event card date↔button swap: visually unconfirmed by operator after final fix — may still need checking
- Auto-translation feature: operator confirmed they want it on ALL pages, but translation API not yet decided (OpenAI key vs free MyMemory). BLOCKED pending answer.
- nightly/2026-03-29 fixes (M8, H1, M6): merged via nightly/2026-03-30 merge — confirmed in main.

## GOTCHAS to Add
- direction:rtl alone does NOT flip flex child order. Always use lex-direction:row-reverse for AR flex containers. Only remove row-reverse if the same element ALSO has an explicit direction:rtl set directly on it (not inherited).
